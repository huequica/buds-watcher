[project]
name = "buds-watcher"
version = "0.1.0"
description = "Sony INZONE Buds 左右切断検知アプリ"
requires-python = ">=3.11"
dependencies = [
    "PySide6>=6.6",
    "hid>=1.0",
]

[tool.uv]
package = false
